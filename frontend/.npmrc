# React 19 ecosystem: several deps still declare React 18 peer ranges even
# though they work on 19. Install in legacy-peer-deps mode so clean installs
# (npm ci / npm install) succeed deterministically.
legacy-peer-deps=true
