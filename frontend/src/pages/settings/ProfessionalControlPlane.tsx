import React, { useCallback, useEffect, useState } from 'react';
import { api } from '../../hooks/useApi';
import type { SettingsTab } from './types';

type Readiness = { status: string; active_model: string; components: Record<string, string>; degraded_reasons: string[]; configuration_revision: number; paper_mode: boolean };
type Props = { tab: SettingsTab };
type SafeOverview = { paper_mode: boolean; human_approval_required: boolean; agents: Array<{ id: string; name: string; mission: string; risk: string; status: string }>; models: Array<{ id: string; role: string; health: string; route: string; secret_status: string }>; integrations: Array<{ id: string; name: string; category: string; status: string; scopes: string[]; token: string }>; pending_proposals: number; capabilities: Record<string, boolean | string> };

const domains: Record<string, string> = {
  'control-brain': 'core_brain', 'control-models': 'models', 'control-agents': 'agents',
  'control-connectors': 'connectors', 'control-sandbox': 'sandbox', 'control-startup': 'startup',
};

export default function ProfessionalControlPlane({ tab }: Props) {
  const [readiness, setReadiness] = useState<Readiness | null>(null);
  const [configuration, setConfiguration] = useState<Record<string, any> | null>(null);
  const [audit, setAudit] = useState<any[]>([]);
  const [error, setError] = useState('');
  const [busy, setBusy] = useState(false);
  const [safe, setSafe] = useState<SafeOverview | null>(null);
  const [taskPrompt, setTaskPrompt] = useState('');
  const [taskMessage, setTaskMessage] = useState('');
  const [integrationMessage, setIntegrationMessage] = useState('');
  const [repairMessage, setRepairMessage] = useState('');

  const isSafeTab = tab.startsWith('safe-');
  const load = useCallback(async () => {
    setError('');
    try {
      const [ready, config] = await Promise.all([api.get<Readiness>('/control-plane/readiness'), api.get('/control-plane/configuration')]);
      if (isSafeTab) setSafe((await api.get<SafeOverview>('/safe-platform/overview')).data);
      setReadiness(ready.data);
      setConfiguration(config.data.configuration);
      if (tab === 'control-audit') setAudit((await api.get('/control-plane/audit')).data.items ?? []);
    } catch (e: any) { setError(e?.response?.data?.detail ?? 'Control plane is unavailable'); }
  }, [tab]);
  useEffect(() => { void load(); }, [load]);

  const reprobe = async () => { setBusy(true); try { await api.post('/control-plane/readiness/reprobe'); await load(); } finally { setBusy(false); } };
  const runSandboxTest = async () => { setBusy(true); try { await api.post('/control-plane/sandbox/test', { command: 'python --version', timeout_seconds: 5 }); await load(); } catch (e: any) { setError(e?.response?.data?.detail ?? 'Sandbox test rejected'); } finally { setBusy(false); } };
  const createTask = async () => { if (taskPrompt.trim().length < 3) { setTaskMessage('Describe the task in at least three characters.'); return; } setBusy(true); setTaskMessage('Submitting a read-only plan request…'); try { await api.post('/safe-platform/supervisor/tasks', { prompt: taskPrompt.trim(), requested_agents: [], allow_external_read: false }); setTaskPrompt(''); setTaskMessage('Task accepted for planning. No tools or external calls were executed.'); await load(); } catch (e: any) { setTaskMessage(e?.response?.data?.detail ?? 'Task request rejected'); } finally { setBusy(false); } };
  const integrationAction = async (integrationId: string, action: 'authorize' | 'revoke' | 'rotate' | 'health_probe') => { setBusy(true); setIntegrationMessage(`Requesting ${action.replace('_', ' ')}…`); try { await api.post('/safe-platform/integrations/action', { integration_id: integrationId, action, scopes: [], reason: `Operator requested ${action.replace('_', ' ')}` }); setIntegrationMessage(`${action.replace('_', ' ')} recorded. Secret values were not submitted or exposed.`); await load(); } catch (e: any) { setIntegrationMessage(e?.response?.data?.detail ?? 'Integration action rejected'); } finally { setBusy(false); } };
  const runDiagnosticReview = async () => { setBusy(true); setRepairMessage('Collecting read-only evidence…'); try { const response = await api.post('/safe-platform/diagnostics/run', { scope: 'platform', include_external: false }); const findings = response.data.findings ?? []; setRepairMessage(`${findings.length} findings collected. No repair was applied.`); } catch (e: any) { setRepairMessage(e?.response?.data?.detail ?? 'Diagnostics unavailable'); } finally { setBusy(false); } };
  const createRepairProposal = async () => { setBusy(true); setRepairMessage('Creating reversible proposal…'); try { await api.post('/safe-platform/proposals', { title: 'Review platform readiness findings', kind: 'repair', scope: 'safe-platform', reason: 'Operator requested a reviewable remediation plan', changes: { mode: 'sandbox_only', live_mutation: false }, evidence_ids: ['paper-boundary', 'model-readiness'], rollback_plan: 'Restore last-known-good checkpoint and rerun sandbox health gates.' }); setRepairMessage('Proposal created for human review. No repair was applied.'); } catch (e: any) { setRepairMessage(e?.response?.data?.detail ?? 'Proposal creation rejected'); } finally { setBusy(false); } };

  if (error) return <div style={styles.alert}><strong>Control plane unavailable</strong><p>{error}</p><button style={styles.button} onClick={() => void load()}>Retry</button></div>;
  if (!readiness || !configuration) return <div style={styles.card}>Loading professional configuration…</div>;
  if (isSafeTab && !safe) return <div style={styles.card}>Loading safe agent platform…</div>;

  if (isSafeTab && safe) {
    const title = tab === 'safe-integrations' ? 'External access and token center' : tab === 'safe-repairs' ? 'Repairs, upgrades, and approvals' : tab === 'safe-chat' ? 'Operator communication' : tab === 'safe-models' ? 'Model router' : tab === 'safe-agents' ? 'Specialist agents' : 'Supervisor control';
    return <div style={styles.stack}><div style={styles.header}><div><div style={styles.eyebrow}>SAFE MULTI-AGENT PLATFORM</div><h2 style={styles.title}>{title}</h2><p style={styles.muted}>Read-only by default. Every consequential action requires attributable human approval.</p></div><span style={badge(safe.paper_mode ? 'paper' : 'blocked')}>{safe.paper_mode ? 'PAPER MODE' : 'LIVE BLOCKED'}</span></div><div style={styles.grid}><div style={styles.card}><div style={styles.row}><strong>Human approval</strong><span style={badge('ready')}>REQUIRED</span></div><p style={styles.muted}>No trade, production mutation, credential rotation, deployment, or self-modification is executed from this console without a reviewed proposal.</p></div><div style={styles.card}><div style={styles.row}><strong>Pending proposals</strong><span style={badge(safe.pending_proposals ? 'degraded' : 'ready')}>{safe.pending_proposals}</span></div><p style={styles.muted}>Review evidence, blast radius, test plan, expiry, and rollback before approval.</p></div></div>{tab === 'safe-repairs' && <div style={styles.card}><div style={styles.eyebrow}>REPAIR REVIEW QUEUE</div><p style={styles.muted}>Diagnostics are read-only. Proposed changes include evidence, scope, rollback, and sandbox-only validation before approval.</p><div style={styles.actionRow}><button style={styles.button} disabled={busy} onClick={() => void runDiagnosticReview()}>Run diagnostics</button><button style={styles.smallButton} disabled={busy} onClick={() => void createRepairProposal()}>Draft repair proposal</button></div><div role="status" aria-live="polite" style={styles.muted}>{repairMessage}</div></div>}{tab === 'safe-supervisor' && <div style={styles.card}><div style={styles.eyebrow}>NEW SUPERVISOR TASK</div><p style={styles.muted}>The supervisor creates a plan first. It cannot call tools, browse externally, trade, or mutate production from this form.</p><textarea aria-label="Supervisor task" value={taskPrompt} onChange={event => setTaskPrompt(event.target.value)} placeholder="Ask the supervisor to inspect a failing service or explain a signal…" style={styles.textarea} /><div style={styles.actionRow}><button style={styles.button} disabled={busy} onClick={() => void createTask()}>{busy ? 'Submitting…' : 'Submit read-only task'}</button><span role="status" aria-live="polite" style={styles.muted}>{taskMessage}</span></div></div>}{tab === 'safe-agents' || tab === 'safe-supervisor' ? <div style={styles.grid}>{safe.agents.map(agent => <div key={agent.id} style={styles.card}><div style={styles.row}><strong>{agent.name}</strong><span style={badge(agent.status)}>{agent.status}</span></div><p style={styles.muted}>{agent.mission}</p><small style={styles.muted}>Risk class: {agent.risk}</small></div>)}</div> : tab === 'safe-models' ? <div style={styles.grid}>{safe.models.map(model => <div key={model.id} style={styles.card}><div style={styles.row}><strong>{model.id}</strong><span style={badge(model.health)}>{model.route}</span></div><p style={styles.muted}>Role: {model.role} · Credentials: {model.secret_status}</p></div>)}</div> : tab === 'safe-integrations' ? <><div style={styles.grid}>{safe.integrations.map(item => <div key={item.id} style={styles.card}><div style={styles.row}><strong>{item.name}</strong><span style={badge(item.status)}>{item.status}</span></div><p style={styles.muted}>Scopes: {item.scopes.join(', ')}</p><small style={styles.muted}>Token values never appear in the browser. Provider: {item.token}</small><div style={styles.actionRow}><button style={styles.smallButton} disabled={busy} onClick={() => void integrationAction(item.id, 'health_probe')}>Probe</button><button style={styles.smallButton} disabled={busy} onClick={() => void integrationAction(item.id, item.status === 'revoked' ? 'authorize' : 'revoke')}>{item.status === 'revoked' ? 'Authorize' : 'Revoke'}</button><button style={styles.smallButton} disabled={busy} onClick={() => void integrationAction(item.id, 'rotate')}>Rotate</button></div></div>)}</div><div role="status" aria-live="polite" style={styles.muted}>{integrationMessage}</div></> : <div style={styles.card}><div style={styles.row}><strong>{tab === 'safe-chat' ? 'Chat and voice capabilities' : 'Safe evolution gate'}</strong><span style={badge('ready')}>AVAILABLE</span></div><p style={styles.muted}>{tab === 'safe-chat' ? 'Streaming, delegation, citations, tool traces, cancellation, and escalation are available when configured. External research requires an authorized connector.' : 'Diagnostics may run read-only. Repairs and upgrades produce reversible proposals with checkpoints and automatic rollback gates.'}</p></div>}</div>;
  }

  const domain = domains[tab];
  const title = tab === 'control-overview' ? 'Operations readiness' : tab === 'control-audit' ? 'Configuration audit' : domain?.replace('_', ' ');
  return <div style={styles.stack}>
    <div style={styles.header}><div><div style={styles.eyebrow}>PROFESSIONAL CONTROL PLANE</div><h2 style={styles.title}>{title}</h2><p style={styles.muted}>Versioned, permissioned configuration with truthful runtime health.</p></div><div style={{ display: 'flex', gap: 8 }}><span style={badge(readiness.status)}>{readiness.status.toUpperCase()}</span><span style={badge(readiness.paper_mode ? 'paper' : 'live')}>{readiness.paper_mode ? 'PAPER MODE' : 'LIVE MODE'}</span></div></div>
    {readiness.degraded_reasons.length > 0 && <div style={styles.warning}><strong>Degraded operation</strong><div>{readiness.degraded_reasons.join(' · ')}</div></div>}
    {tab === 'control-overview' && <><div style={styles.grid}>{Object.entries(readiness.components).map(([name, state]) => <div key={name} style={styles.card}><div style={styles.row}><strong>{name.replace('_', ' ')}</strong><span style={badge(state)}>{state}</span></div><div style={styles.muted}>Lifecycle component</div></div>)}</div><div style={styles.card}><div style={styles.row}><strong>Active model route</strong><code>{readiness.active_model}</code></div><div style={styles.muted}>Revision {readiness.configuration_revision}. Secrets remain redacted.</div></div><button style={styles.button} disabled={busy} onClick={() => void reprobe()}>{busy ? 'Checking…' : 'Re-run readiness probes'}</button></>}
    {domain && <div style={styles.card}><pre style={styles.pre}>{JSON.stringify(configuration[domain], null, 2)}</pre>{domain === 'sandbox' && <button style={styles.button} disabled={busy} onClick={() => void runSandboxTest()}>Run safe paper-mode test</button>}</div>}
    {tab === 'control-audit' && <div style={styles.card}>{audit.length === 0 ? <div style={styles.muted}>No configuration mutations recorded.</div> : audit.map(item => <div key={item.id} style={styles.audit}><strong>{item.action}</strong><span>{item.result}</span><small>{item.reason} · {new Date(item.created_at).toLocaleString()}</small></div>)}</div>}
  </div>;
}

const badge = (value: string): React.CSSProperties => ({ color: value === 'ready' || value === 'paper' ? '#34d399' : value === 'degraded' ? '#fbbf24' : '#94a3b8', fontSize: 11, fontWeight: 800, letterSpacing: '.08em' });
const styles: Record<string, React.CSSProperties> = { stack: { display: 'flex', flexDirection: 'column', gap: 16 }, header: { display: 'flex', justifyContent: 'space-between', gap: 20, alignItems: 'flex-start' }, eyebrow: { color: '#60a5fa', fontSize: 11, fontWeight: 800, letterSpacing: '.12em' }, title: { margin: '6px 0', color: '#f8fafc', fontSize: 25, textTransform: 'capitalize' }, muted: { color: '#94a3b8', fontSize: 13, lineHeight: 1.5 }, grid: { display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))', gap: 12 }, card: { background: '#0f172a', border: '1px solid #1e293b', borderRadius: 12, padding: 16 }, row: { display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 12, color: '#e2e8f0' }, warning: { padding: 14, borderRadius: 10, border: '1px solid #92400e', background: '#451a03', color: '#fef3c7', fontSize: 13, lineHeight: 1.5 }, alert: { padding: 20, borderRadius: 12, border: '1px solid #7f1d1d', background: '#450a0a', color: '#fecaca' }, button: { border: 0, borderRadius: 8, padding: '10px 14px', background: '#2563eb', color: '#fff', fontWeight: 700, cursor: 'pointer' }, textarea: { width: '100%', minHeight: 92, marginTop: 12, padding: 12, borderRadius: 8, border: '1px solid #334155', background: '#020617', color: '#e2e8f0', resize: 'vertical', fontFamily: 'inherit', boxSizing: 'border-box' }, actionRow: { display: 'flex', alignItems: 'center', gap: 12, flexWrap: 'wrap', marginTop: 12 }, smallButton: { border: '1px solid #334155', borderRadius: 7, padding: '6px 9px', background: '#111827', color: '#cbd5e1', fontWeight: 700, cursor: 'pointer', fontSize: 11 }, pre: { margin: 0, whiteSpace: 'pre-wrap', color: '#cbd5e1', fontSize: 12, fontFamily: 'monospace' }, audit: { display: 'grid', gridTemplateColumns: '1fr auto', gap: 5, padding: '12px 0', borderBottom: '1px solid #1e293b', color: '#e2e8f0' } };
