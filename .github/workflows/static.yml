# Documentation is deployed via docs.yml using mkdocs gh-deploy.
# This workflow is kept as a no-op to avoid breaking any external references.
# See docs.yml for the active deployment workflow.
name: Deploy static content to Pages

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Docs deployed via docs.yml
        run: |
          echo "Documentation deployment is handled by docs.yml (mkdocs gh-deploy)."
          echo "This workflow is a no-op. Trigger docs.yml instead."
