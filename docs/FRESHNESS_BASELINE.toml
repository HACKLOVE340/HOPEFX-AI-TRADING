# Freshness baseline — Group 3 Chapter 15. The ratchet.
#
# Findings that already existed when the check was adopted. A NEW finding
# blocks; these do not. Removing a line here is how the debt is paid down,
# and a line whose finding is fixed must be removed or it hides a recurrence.
#
# Regenerate:  python scripts/docs_freshness.py --adopt
# Check:       python scripts/docs_freshness.py --check

adopted = "2026-09-08"

# Paid down to zero on 2026-09-18. Of the 36 entries adopted, 29 were reported
# live and every one was corrected in the document it named; the remaining 7 had
# already stopped being reported — README.md's `docs/FEATURES.md` was corrected
# to `docs/archive/FEATURES.md` earlier, and the other six name runtime artefacts
# (model outputs, `config/risk_appetite.json`, `logs/*.log`) that git now ignores,
# so the checker's ignored-path guard covers them. A new finding blocks.
known = []
