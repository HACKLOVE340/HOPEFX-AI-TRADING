{
  "name": "hopefx-ai-gateway-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Vercel AI Gateway example. Deliberately NOT part of frontend/ — generateText is a server-side call and AI_GATEWAY_API_KEY must never reach the browser bundle.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "example": "node --env-file=.env.local index.ts"
  },
  "dependencies": {
    "ai": "^7.0.99"
  }
}
