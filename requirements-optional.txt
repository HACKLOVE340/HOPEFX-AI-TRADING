# HOPEFX AI Trading Platform — Optional Dependencies
#
# These packages require system libraries, have platform restrictions, or are
# heavy GPU/ML deps that are NOT installed in CI. Install manually after
# satisfying the system prerequisites listed for each group.
#
# DO NOT pin exact versions here — use minimum bounds so Dependabot can
# propose security upgrades. Exact pins belong in requirements.txt for
# packages that are always installed.

# ── TA-Lib ────────────────────────────────────────────────────────────────────
# Requires the libta-lib C library.
# Ubuntu/Debian: sudo apt-get install libta-lib-dev
# macOS:         brew install ta-lib
# Then:          pip install ta-lib>=0.4.28
# ta-lib>=0.4.28

# ── TensorFlow ────────────────────────────────────────────────────────────────
# Large GPU/CPU deep-learning framework. Install separately on GPU nodes.
# pip install tensorflow>=2.16.0
# tensorflow>=2.16.0

# ── PyTorch ───────────────────────────────────────────────────────────────────
# Install via the official index for the correct CUDA version:
# CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121
# torch>=2.1.0

# ── vectorbt ──────────────────────────────────────────────────────────────────
# Backtesting library with optional Numba JIT acceleration.
# pip install vectorbt>=0.26.0
# vectorbt>=0.26.0

# ── MetaTrader5 ───────────────────────────────────────────────────────────────
# Windows-only broker SDK. The MT5 terminal must be installed on the same machine.
# Not available on Linux/macOS — the package will fail to import on those platforms.
# start.bat and start.ps1 install this automatically on Windows.
# Manual install: pip install --no-cache-dir "MetaTrader5>=5.0.45"
MetaTrader5>=5.0.45

# ── Zipline ───────────────────────────────────────────────────────────────────
# Has strict Python version constraints; check compatibility before installing.
# zipline-reloaded>=3.0.0

# ── MLflow ────────────────────────────────────────────────────────────────────
# Experiment tracking server. Optional for production deployments.
# pip install mlflow>=2.13.0
# mlflow>=2.13.0

# ── Alpaca Trade API ──────────────────────────────────────────────────────────
# US equities broker SDK.
# pip install alpaca-trade-api>=3.3.0
# alpaca-trade-api>=3.3.0

# ── Binance Connector ─────────────────────────────────────────────────────────
# pip install binance-connector>=3.8.0
# binance-connector>=3.8.0

# ── Celery ────────────────────────────────────────────────────────────────────
# Distributed task queue for periodic background jobs (ML retraining,
# subscription expiry, affiliate payouts, DB backups, self-healer scans).
# Required only when running dedicated Celery worker + Beat processes.
# The FastAPI app falls back to asyncio tasks when Celery is absent.
celery[redis]>=5.4.0

# ── asyncio-mqtt ──────────────────────────────────────────────────────────────
# MQTT async client. Required only for IoT/signal integrations.
# pip install asyncio-mqtt>=0.16.2
# asyncio-mqtt>=0.16.2

# ── kaleido ───────────────────────────────────────────────────────────────────
# Static image export for Plotly. Required only for report generation.
# pip install kaleido>=0.2.1
# kaleido>=0.2.1

# ── pandas-ta ─────────────────────────────────────────────────────────────────
# Pure-Python technical analysis library (alternative to TA-Lib).
# pip install pandas-ta>=0.3.14b0
# pandas-ta>=0.3.14b0

# ── fastapi-offline ───────────────────────────────────────────────────────────
# Service worker / PWA generation helper.
# pip install fastapi-offline>=1.0.0
# fastapi-offline>=1.0.0

# ── hiredis ───────────────────────────────────────────────────────────────────
# C-extension parser for redis-py (performance). Falls back gracefully.
# pip install hiredis>=2.3.0
# hiredis>=2.3.0

# ── polars ────────────────────────────────────────────────────────────────────
# Fast DataFrame library. Optional alternative to pandas for heavy ETL.
# pip install polars>=0.20.3
# polars>=0.20.3
