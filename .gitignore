# Kill switch flag — runtime state, never commit
kill_switch.flag

# Runtime state directory — persisted state files, never commit
state/

# FIX session config with live credentials — never commit
fix.cfg.local

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/

# MkDocs generated site (built by CI, not committed)
site/

# Frontend build output and dependencies
frontend/node_modules/
static/

# Keep the built dashboard so the server can serve it without a build step
!dashboard/dist/
downloads/
eggs/
.eggs/
lib/
lib64/
# Source lib directories inside frontend packages are not Python venvs
!frontend/src/lib/
!dashboard/src/lib/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Node
node_modules/
dashboard/node_modules/
# Ignore root-level lockfile (Python project root) but track frontend lockfile for CI caching
/package-lock.json
dashboard/package-lock.json
# Explicitly track frontend lockfile
!frontend/package-lock.json

# Virtual environments
venv/
env/
ENV/
.venv/
.env
.venv
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints
# *.ipynb  — tracked for examples/

# Environment variables
.env
.env.local
.env.*.local
# deployments/gen_env.sh and vps_bootstrap.sh move a previous .env aside before
# writing a new one. Those backups hold REAL secrets — never let them be
# committed. Covers .env.bak.<timestamp>, .env.old, and similar.
.env.bak
.env.bak.*
.env.old
.env.save
# deployments/gen_env.sh also writes .env.paste.txt — the same secrets with
# comments stripped, for pasting into a hosting panel's Environment form.
.env.paste.txt
.env.paste.min.txt
!.env.example

# Database
*.db
*.sqlite
# SQLite write-ahead-log / shared-memory sidecar files (transient, local-only)
*.db-shm
*.db-wal
*.db-journal

# Generated platform-doctor health reports (local diagnostics, not source)
diagnostics/
*.sqlite3
# data/ — source package, not ignored
# /data/  # uncomment to ignore data download cache
*.dump

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Operational config files — contain strategy internals and may hold credentials.
# These are generated/edited at deploy time; never commit live values.
# Copy from *.example templates and fill in locally.
WORDMAP.json
!WORDMAP.json.example
# prop_firm_mode.json is committed with placeholder credentials for CI.
# Override locally with real credentials — do NOT commit real tokens.
!prop_firm_mode.json.example

# Signed risk-appetite policy — the live, governance-approved override stays
# local; only the template is committed.
config/risk_appetite.json
!config/risk_appetite.example.json

# ML trained model artifacts (not source code)
/models/
# Binary model artifacts — regenerated by training; never commit
ml/saved_models/*.pkl
!ml/saved_models/current.pkl
!ml/saved_models/advanced_oos.pkl
!ml/saved_models/hybrid_meta.pkl
!ml/saved_models/rf_macro.pkl
!ml/saved_models/rf_macro_oos.pkl
!ml/saved_models/rf_xauusd.pkl
!ml/saved_models/stacking_ensemble.pkl
!ml/saved_models/xgb_macro.pkl
!ml/saved_models/xgb_macro_oos.pkl
!ml/saved_models/feature_scaler.pkl
ml/saved_models/*.joblib
ml/saved_models/*_training_report.json
ml/saved_models/*_meta.json
ml/saved_models/ab_baseline_report.json
# model_checksums.json is tracked so CI can verify model integrity
# !ml/saved_models/model_checksums.json

# Rendered tutorial media is heavy + hosted externally; the small storyboard
# manifests + registry under tutorials/generated/ ARE tracked so the Academy can
# serve generation status without re-running the pipeline.
tutorials/generated/*.mp4
tutorials/generated/*.mov
tutorials/generated/*.wav
tutorials/generated/*.mp3
tutorials/generated/*.png

# Runtime-generated data files
backtest/results/
data/paper_trading_gate.json
data/oanda_paper_start.json
data/heal_manifest.json
data/rollback_history.json
data/regulatory_dlq/
# registry.json is tracked in git as the seed manifest; updated by ModelRegistry at runtime
*.h5
*.pt
*.pth
mlruns/
mlflow.db

# Redis
*.rdb
dump.rdb

# Docker
.docker/
docker-compose.override.yml

# Kubernetes
*.kubeconfig
k8s-secrets/
# Real k8s secret files with actual credentials (use *.local.yaml naming)
k8s/*.local.yaml
k8s/k8s-secrets.filled.yaml

# Testing
.coverage
coverage.xml
# pytest JUnit output — generated by CI (--junitxml), uploaded as an artifact; never commit
test-results.xml
htmlcov/
.tox/
.nox/
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.hypothesis/

# Build artifacts
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt

# Security
secrets/
credentials/
*.pem
*.key
*.crt
.encryption_key

# Temporary files
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Project specific
backtest_results/
trading_logs/
# cache/ is a source code package — not ignored
# Ignore only runtime cache artifacts:
*.cache

# Kill switch runtime flag
kill_switch.flag
kill_switch.state.json

# Downloaded OHLCV cache files (yfinance, large data)
data/*Y.csv
!data/XAUUSD_50Y.csv
!data/XAUUSD_40Y.csv
# Runtime market data fetched by the scheduler — not source files
data/*_M1.csv
data/*_M5.csv
data/*_M15.csv
data/*_M30.csv
data/*_H1.csv
data/*_H4.csv
data/*_D.csv
data/*_W.csv
data/av_last_scan.json
data/macro/*.csv
frontend/coverage/

# Data layer runtime artifacts (generated at runtime, not source)
data/lineage/
data/dukascopy_cache/
data/fred_cache/
data/macro/*.csv
-e
# Admin bootstrap credential files (create_admin.py, create_superadmin.py)
admin_password.txt
superadmin_credentials.txt
.python-version
mutants/

# Runtime-generated audit logs
data/audit/

# Playwright test artifacts
frontend/test-results/
frontend/playwright-report/

# ── Credential / secret files — never commit ─────────────────────────────────
# Any file matching these patterns contains real credentials.
# Use .env.example or *.example for templates.
*.local.yaml
*.local.yml
*.local.env
*.secrets
*.secret
*_credentials.json
*_credentials.yaml
*_credentials.yml
broker_config.local.*
brokers.local.yaml
fix.cfg.local
oanda.local.*
ibkr.local.*
mt5.local.*
alpaca.local.*
binance.local.*

# Docker compose overrides with real credentials
docker-compose.override.yml
docker-compose.local.yml
docker-compose.prod.yml

# Paper trading state (contains account snapshots)
paper_trading_state.json
paper_run_*.json
-e
# Auto-generated at runtime by security/antivirus.py
security/yara_rules/

# Playwright sweep session (contains a live token)
frontend/.auth.json

# k6 load-test output. The directory is tracked (via .gitkeep) because CI writes
# summaries into it and tests/unit/test_k6_load_tests.py checks it exists; the
# run artifacts themselves are per-run and belong in the workflow's upload.
k6/results/*
!k6/results/.gitkeep

# Runtime state written by the paper-trading gate and the regulatory DLQ during
# a test run or a local session. Both are per-deployment state, not source.
data/paper_gate_state.json
data/reg_dlq/
